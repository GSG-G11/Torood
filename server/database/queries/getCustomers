const { pool } = require('../config/connection');

const getCustomers = () => {
  return pool.query({
    text: 'SELECT name FROM customers;',
    values: [],
  });
};

module.exports = { getCustomers };
